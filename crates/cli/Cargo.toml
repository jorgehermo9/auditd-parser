[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
auditd-parser = { path = "../..", features = ["serde"] }
clap = { version = "4", features = ["derive"] }
serde_json = "1.0.143"
